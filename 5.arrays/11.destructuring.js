// a regular old array
const someNumbers = [10, 20, 30, 40, 50]

// values in an array can be extracted into separate variables upon assignment:
const [a, b] = someNumbers // a=10, b=20

console.log(`The value of a=${a} and b=${b}.`)

const numbers = [65, 44, 12, 4]

/*
arrays have a built-in .forEach function
it accepts a function definition as its argument
that function is called for each value in the array
*/
numbers.forEach(n => {
  console.log(`The value is ${n}`)
})

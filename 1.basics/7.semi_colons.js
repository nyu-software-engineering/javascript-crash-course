const semi = 'colons'; // a statement with a semi-colon at the end

const optional = true //  a statement without the semi-colon at the end...

// the general rule
console.log(`semi-${semi} are ${optional ? 'optional' : 'not optional'} in Javascript!`)

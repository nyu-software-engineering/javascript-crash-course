// an arrow function with only one parameter... no parentheses are required around it
const doSomething2 = x => {
  console.log(`doSomething2 is running with x=${x}.`)
}

doSomething2("hello") // call the function with one argument
